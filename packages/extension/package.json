{
  "name": "@streamdockbridge/extension",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "esbuild src/background.ts src/content.ts --bundle --platform=browser --target=chrome110 --outdir=dist && tsc -b",
    "typecheck": "tsc -b --noEmit"
  },
  "devDependencies": {
    "esbuild": "^0.28.2"
  }
}