{
  "name": "@streamdockbridge/extension",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "tsc -b && esbuild src/background.ts src/content.ts --bundle --platform=browser --target=chrome110 --outdir=dist",
    "typecheck": "tsc -b --noEmit"
  },
  "devDependencies": {
    "esbuild": "^0.28.2"
  }
}